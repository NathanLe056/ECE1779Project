apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  type: NodePort # Changed from LoadBalancer
  selector:
    app: api
  ports:
    - protocol: TCP
      port: 3000 # Internal cluster port
      targetPort: 3000
      nodePort: 32000 # Use this to access K8s API via localhost:32000
