apiVersion: v1
kind: Service
metadata:
  name: db-service
spec:
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5433        # This is the port you'll use from your laptop
      targetPort: 5432  # This is the port inside the pod