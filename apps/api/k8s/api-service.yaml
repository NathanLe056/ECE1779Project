apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  type: LoadBalancer # This maps the cluster port to your actual localhost
  selector:
    app: api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000