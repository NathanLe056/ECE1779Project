{
  "comment": "Merge these dependency additions into your existing apps/api/package.json",

  "dependencies_to_add": {
    "ws": "^8.18.0",
    "redis": "^4.7.0"
  },

  "devDependencies_to_add": {
    "@types/ws": "^8.5.13"
  },

  "example_full_dependencies_section": {
    "note": "Shown for context. Only the three packages above are new.",
    "dependencies": {
      "express": "^4.18.0",
      "jsonwebtoken": "^9.0.0",
      "ws": "^8.18.0",
      "redis": "^4.7.0"
    },
    "devDependencies": {
      "@types/express": "^4.17.0",
      "@types/jsonwebtoken": "^9.0.0",
      "@types/ws": "^8.5.13",
      "typescript": "^5.4.0",
      "ts-node": "^10.9.0"
    }
  },

  "install_commands": {
    "npm":  "npm install ws redis && npm install -D @types/ws",
    "pnpm": "pnpm add ws redis && pnpm add -D @types/ws",
    "yarn": "yarn add ws redis && yarn add -D @types/ws"
  }
}
