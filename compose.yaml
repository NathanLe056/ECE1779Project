services:
  api:
    build: ./apps/api
    ports:
      - "3001:3000"
    environment:
      - DB_HOST=db
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}?schema=public
      - JWT_SECRET=${JWT_SECRET}
    depends_on:
      db:
        condition: service_healthy
        restart: true

    networks:
      - app-network

  frontend:
    build: ./apps/web
    ports:
      - "5173:80"
    depends_on:
      db:
        condition: service_healthy
        restart: true

    networks:
      - app-network
  db:
    ports:
      - "5433:5432"          #because we want docker compose on a different port than k8s
    image: postgres:17
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - app-network
  

networks:
  app-network:
    driver: bridge

volumes:
  db-data: